# Database: Supabase PostgreSQL. Set DATABASE_URL in .env (Supabase connection string).
services:
  app:
    image: node:20-alpine
    working_dir: /app
    command: sh -c "npm install && npm run dev"
    env_file:
      - ./.env.local
    environment:
      NEXTAUTH_URL: http://localhost:3000
    volumes:
      - ./src:/app
      - app_node_modules:/app/node_modules
    ports:
      - "3000:3000"

volumes:
  app_node_modules:
