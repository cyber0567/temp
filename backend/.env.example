# -----------------------------------------------------------------------------
# Backend environment variables – copy to .env and fill in values.
# -----------------------------------------------------------------------------

# Server
PORT=3001
NODE_ENV=development

# Supabase DB only (auth is custom JWT + Passport Google; run migration 003_custom_auth_users.sql)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# URLs
FRONTEND_URL=http://localhost:3000
API_BASE_URL=http://localhost:3001

# Session / JWT (e.g. openssl rand -hex 32)
SESSION_SECRET=

# Google OAuth (Google Cloud Console > APIs & Services > Credentials)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Must match "Authorized redirect URI" in Google Console (e.g. http://localhost:3001/auth/google/callback)
CALLBACK_URL=http://localhost:3001/auth/google/callback

# RingCentral OAuth (RingCentral Developer Portal > Apps)
RINGCENTRAL_CLIENT_ID=
RINGCENTRAL_CLIENT_SECRET=
# Sandbox: https://platform.devtest.ringcentral.com | Prod: https://platform.ringcentral.com
RINGCENTRAL_SERVER=https://platform.ringcentral.com
# Must match "Redirect URI" in RingCentral app (e.g. http://localhost:3001/auth/ringcentral/callback)
RINGCENTRAL_CALLBACK_URL=http://localhost:3001/auth/ringcentral/callback
